% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{dc_download}
\alias{dc_download}
\title{Download all rows of a dataset to a CSV file}
\usage{
dc_download(
  client,
  dataset_id,
  output_path,
  start = NULL,
  end = NULL,
  symbols = NULL,
  limit = 100L,
  verbose = interactive(),
  ...
)
}
\arguments{
\item{client}{A \code{datacore_client} created with \code{\link[=datacore_client]{datacore_client()}}.}

\item{dataset_id}{Character. The dataset id, e.g. \code{"equity.vn30.daily"}.}

\item{output_path}{Character. File path for the CSV output.}

\item{start}{Optional. Start date (inclusive). A \code{Date}, \code{POSIXt}, or
\code{"yyyy-mm-dd"} string.}

\item{end}{Optional. End date (inclusive).}

\item{symbols}{Optional. Character vector of ticker symbols to filter by.}

\item{limit}{Integer. Rows per page. Defaults to \code{100L}.}

\item{verbose}{Logical. Print progress. Defaults to \code{interactive()}.}

\item{...}{Additional named arguments passed as query parameters.}
}
\value{
Invisibly returns a list with \code{output_path}, \code{pages_downloaded},
and \code{rows_downloaded}.
}
\description{
Paginates through the dataset and writes each page to \code{output_path} as
it arrives, so memory usage stays constant regardless of dataset size.
}
\examples{
\dontrun{
  dc <- datacore_client()
  dc_download(dc, "equity.vn30.daily",
              output_path = "vn30_2024.csv",
              start = "2024-01-01", end = "2024-12-31")
}

}
\seealso{
\code{\link[=dc_collect]{dc_collect()}} to load all rows into memory.
}

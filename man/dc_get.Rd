% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{dc_get}
\alias{dc_get}
\title{Get one page of rows from a dataset}
\usage{
dc_get(
  client,
  dataset_id,
  start = NULL,
  end = NULL,
  symbols = NULL,
  page = 1L,
  limit = 100L,
  ...
)
}
\arguments{
\item{client}{A \code{datacore_client} created with \code{\link[=datacore_client]{datacore_client()}}.}

\item{dataset_id}{Character. The dataset id, e.g. \code{"equity.vn30.daily"}.}

\item{start}{Optional. Start date (inclusive). A \code{Date}, \code{POSIXt}, or
\code{"yyyy-mm-dd"} string.}

\item{end}{Optional. End date (inclusive). A \code{Date}, \code{POSIXt}, or
\code{"yyyy-mm-dd"} string.}

\item{symbols}{Optional. Character vector of ticker symbols to filter by.
Will be comma-joined for the \code{symbols=} query parameter.}

\item{page}{Integer. Page number (1-based). Defaults to \code{1L}.}

\item{limit}{Integer. Rows per page. Defaults to \code{100L} (server maximum).}

\item{...}{Additional named arguments passed through as query parameters.}
}
\value{
A tibble. Columns depend on the dataset; see \code{\link[=dc_schema]{dc_schema()}}.
}
\description{
Fetches a single page of rows from a dataset. Use \code{\link[=dc_collect]{dc_collect()}} to
fetch all pages automatically, or iterate manually with
\code{page = 1L, 2L, ...}.
}
\examples{
\dontrun{
  dc <- datacore_client()

  # First page of VN30 daily
  pg1 <- dc_get(dc, "equity.vn30.daily",
                start = "2024-01-01", page = 1L, limit = 100L)

  # For all pages at once, use dc_collect()
  all <- dc_collect(dc, "equity.vn30.daily", start = "2024-01-01")
}

}
\seealso{
\code{\link[=dc_collect]{dc_collect()}} to auto-paginate, \code{\link[=dc_download]{dc_download()}} to save to disk.
}

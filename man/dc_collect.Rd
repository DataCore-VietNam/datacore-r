% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{dc_collect}
\alias{dc_collect}
\title{Fetch all rows from a dataset, paginating automatically}
\usage{
dc_collect(
  client,
  dataset_id,
  start = NULL,
  end = NULL,
  symbols = NULL,
  limit = 100L,
  max_rows = NULL,
  verbose = interactive(),
  ...
)
}
\arguments{
\item{client}{A \code{datacore_client} created with \code{\link[=datacore_client]{datacore_client()}}.}

\item{dataset_id}{Character. The dataset id, e.g. \code{"equity.vn30.daily"}.}

\item{start}{Optional. Start date (inclusive). A \code{Date}, \code{POSIXt}, or
\code{"yyyy-mm-dd"} string.}

\item{end}{Optional. End date (inclusive).}

\item{symbols}{Optional. Character vector of ticker symbols to filter by.}

\item{limit}{Integer. Rows per page. Defaults to \code{100L}.}

\item{max_rows}{Integer or \code{NULL}. Stop after this many rows. \code{NULL} (the
default) fetches everything.}

\item{verbose}{Logical. Print a progress line per page. Defaults to
\code{interactive()}.}

\item{...}{Additional named arguments passed as query parameters.}
}
\value{
A single tibble combining all pages.
}
\description{
Repeatedly calls the API until all pages are retrieved (or \code{max_rows} is
reached) and returns a single combined tibble.
}
\examples{
\dontrun{
  dc <- datacore_client()

  # Fetch all VN30 daily data for 2024
  vn30 <- dc_collect(dc, "equity.vn30.daily",
                     start = "2024-01-01", end = "2024-12-31")

  # Cap at 500 rows for exploration
  sample <- dc_collect(dc, "equity.vn30.daily",
                       start = "2023-01-01", max_rows = 500L)
}

}
\seealso{
\code{\link[=dc_get]{dc_get()}} for a single page, \code{\link[=dc_download]{dc_download()}} to save to disk.
}
